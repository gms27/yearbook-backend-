info:
  name: Deletar mensagem
  type: http
  seq: 9

http:
  method: DELETE
  url: https://fluffy-barnacle-97pjgp957xxjf996q-3000.app.github.dev/mensagens/2
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 204", function() {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
